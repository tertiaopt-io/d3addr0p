parameters:
    level: max
    paths:
        - src
        - public
        - tests
    # `mixed` is permitted only at I/O boundaries and must be narrowed immediately (§6).
    treatPhpDocTypesAsCertain: true
